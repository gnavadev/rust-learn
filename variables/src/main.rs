fn main() {
    // let bunies = 2;
    // let (dogs, bones) = (8, 50);
    // let mut cats = 32; //mut makes the variable mutable
    // cats = 29;
    // const BOLT_DOGS: f64 = 9.9; //Use SCREAMING_SNAKE_CASE, value needs a type ex: f64 and neets to be constant
}
